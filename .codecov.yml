comment:
  layout: 'condensed_header, condensed_files, flags, condensed_footer'
  behavior: once
  require_changes: true
coverage:
  range: 70..100
  round: down
  precision: 2
  status:
    patch:
      default:
        target: auto
        threshold: 5%
        flags:
          - app-blog
          - lib-ng-blog-container
          - lib-ng-services
          - lib-ng-styles
          - lib-ng-ui-elements
          - lib-ts-utils
    project:
      default:
        target: auto
        threshold: 5%
        flags:
          - app-blog
          - lib-ng-blog-container
          - lib-ng-services
          - lib-ng-styles
          - lib-ng-ui-elements
          - lib-ts-utils
flags:
  app-blog:
    paths:
      - apps/blog
  lib-ng-blog-container:
    paths:
      - ng-features/blog-container
  lib-ng-services:
    paths:
      - ng-libs/services
  lib-ng-styles:
    paths:
      - ng-libs/styles
  lib-ng-ui-elements:
    paths:
      - ng-libs/ui-elements
  lib-ts-utils:
    paths:
      - ts-libs/ts-utils
